defaults:
  - sim: sim_default
  - controller: koopman
  - _self_

hydra:
  run:
    dir: outputs/evaluate/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: outputs/evaluate/sweep/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}